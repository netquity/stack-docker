version: '2'

services:

  elasticsearch:  # https://github.com/elastic/elasticsearch-docker
    build:
      context: ./elasticsearch/
    container_name: elasticsearch
    volumes:
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
    ports:  # exposed to localhost only
      - "127.0.0.1:9200:9200"
      - "127.0.0.1:9300:9300"
    environment:
      ES_JAVA_OPTS: "-Xmx256m -Xms256m"
      PING_USERNAME: "kibanaro"  # TODO: use env vars or secrets here
      PING_PASSWORD: "kibanaro"
    networks:
      - elk

  kibana:  # https://github.com/elastic/kibana-docker
    build:
      context: kibana/
    container_name: kibana
    volumes:
      - ./kibana/config/:/usr/share/kibana/config:ro
    ports:
      - "5601:5601"
    networks:
      - elk
    depends_on:
      - elasticsearch

  apm_server:  # https://github.com/elastic/apm-server-docker
    build:
      context: apm-server
    container_name: apm_server
    ports: ['8200:8200']
    networks:
      - elk
    command: -e -E 'apm-server.host=0.0.0.0:8200'
    depends_on: ['elasticsearch', 'kibana']

networks:

  elk:
    driver: bridge
